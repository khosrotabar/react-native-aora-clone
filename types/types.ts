import { Models } from "react-native-appwrite";

export type LoginFormProps = {
  email: string;
  password: string;
};

export type SignUpFormProps = {
  username: string;
  email: string;
  password: string;
};
